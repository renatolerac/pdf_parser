Selecione o pdf

<%= form_tag({:action => :upload}, :multipart => true) do |f| %>
  <%= file_field_tag 'pdf' %>
  <%= submit_tag 'Enviar' %>
<% end %>


<% @output.each do |string| %>
  <%= string %>
  </br>
<% end unless @output.blank? %>