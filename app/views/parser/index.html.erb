Selecione o pdf

<%= form_tag({:action => :upload}, :multipart => true) do |f| %>
  <%= file_field_tag 'pdf' %>
  <%= submit_tag 'Enviar' %>
<% end %>


<% @duties.each do |duty| %>
  <hr>
  <% duty.to_s.split("\n").each do |attr| %>
    <%= attr %>
      </br>
  <% end %>
<% end unless @duties.blank? %>